# AWS Configuration for SES Email Service
# Copy this file to .env and fill in your actual values

# AWS Region where SES is configured (e.g., us-east-1, us-west-2, eu-west-1)
AWS_REGION=us-east-1

# AWS Credentials (required for SES authentication)
# Note: Never commit your actual credentials to version control
AWS_ACCESS_KEY_ID=your_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# SES Configuration Notes:
# 1. Verify your sender email address in the AWS SES console
# 2. If in sandbox mode, recipient addresses must also be verified
# 3. Request production access to remove sandbox restrictions
# 4. Ensure IAM user/role has ses:SendEmail permission
